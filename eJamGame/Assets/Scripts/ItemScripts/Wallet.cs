using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Wallet : ItemClass
{
    // Start is called before the first frame update
    void Start()
    {
        name = "Wallet";
        ID = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
