using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class checkbooksplash : ItemClass
{
    // Start is called before the first frame update
    void Start()
    {
        name = "Check Book Splash";
        ID = 3;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
