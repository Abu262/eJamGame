using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class checkbook : ItemClass
{
    // Start is called before the first frame update
    void Start()
    {
        name = "Checkbook";
        ID = 6;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
