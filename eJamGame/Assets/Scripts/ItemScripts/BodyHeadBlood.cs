using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BodyHeadBlood : ItemClass
{
    // Start is called before the first frame update
    void Start()
    {
        name = "Body - Bloody Head";
        ID = 9;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
