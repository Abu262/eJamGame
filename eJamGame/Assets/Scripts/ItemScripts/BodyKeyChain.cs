using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BodyKeyChain : ItemClass
{
    // Start is called before the first frame update
    void Start()
    {
        name = "Book Keychain";
        ID = 7;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
